{% extends 'base.html.twig' %}

{# prefetch data for next usage in marionette #}
{% block prefetch %}
	{% if prefetch %}
		window.prefetch = JSON.parse( unescape( '{{ prefetch | prefetch(['list']) | escape('js') }}' ) );
	{% endif %}
{% endblock %}

{% block body %}
	
{% endblock %}